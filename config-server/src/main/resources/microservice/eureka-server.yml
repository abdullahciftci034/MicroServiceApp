server:
  port: 8761
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  server:
    enableSelfPreservation: false
  serviceUrl:
    defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: "*"